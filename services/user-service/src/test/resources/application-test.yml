spring:
#  datasource:
#    hikari:
#      jdbc-url: jdbc:postgresql://localhost:5433/testdb
#      username: test
#      password: test
#      driver-class-name: org.postgresql.Driver
#      connection-timeout: 20000
#      maximum-pool-size: 10
#      minimum-idle: 5
#      idle-timeout: 300000
#      max-lifetime: 1200000
#      auto-commit: false

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers}

  cloud:
    vault:
      enabled: false

logging:
  level:
    com.example: DEBUG
    org.testcontainers: WARN